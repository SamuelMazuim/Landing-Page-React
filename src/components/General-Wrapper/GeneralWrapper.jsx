import React from "react";
import { StyledGeneralWrapper } from "./StyledGeneralWrapper";

export const GeneralWrapper = (props) => {
    return <StyledGeneralWrapper>{props.children}</StyledGeneralWrapper>;
};
